<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.smart.mapper.ShoppingCartMapper">
    <select id="list" resultType="com.smart.entity.ShoppingCart">
        select * from shopping_cart
        where user_id = #{userId}
        <if test="dishId != null">
            and dish_id = #{dishId}
        </if>
        <if test="dishFlavor != null">
            and dish_flavor = #{dishFlavor}
        </if>
    </select>
    <select id="getByDishIdAndDishFlavor" resultType="com.smart.entity.ShoppingCart">
        select id, name, image, user_id, dish_id, dish_flavor, number, amount, create_time
        from shopping_cart
        where dish_id = #{dishId}
        <if test="dishFlavor != null">
            and dish_flavor = #{dishFlavor}
        </if>
    </select>
</mapper>
